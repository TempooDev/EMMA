openapi: 3.0.0
info:
  title: EMMA Ingest API
  description: API for the EMMA Ingestion Service. Primarily used for monitoring status and inspecting ingested data metrics.
  version: 1.0.0
paths:
  /health:
    get:
      summary: Health Check
      description: Returns the health status of the service.
      responses:
        '200':
          description: Service is healthy
          content:
            text/plain:
              schema:
                type: string
                example: OK
